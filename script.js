const promptInput = document.getElementById("prompt"); const chat = document.getElementById("chat"); const sendButton = document.getElementById("send"); function addMessage(text, type) { const welcome = document.getElementById("welcome"); if (welcome) { welcome.remove(); } const message = document.createElement("div"); message.className = "message"; const avatar = document.createElement("div"); avatar.className = "avatar " + (type === "user" ? "user-avatar" : "ai-avatar"); avatar.textContent = type === "user" ? "You" : "AI"; const content = document.createElement("div"); content.className = "content"; content.textContent = text; message.appendChild(avatar); message.appendChild(content); chat.appendChild(message); chat.scrollTop = chat.scrollHeight; return content; } async function sendMessage() { const text = promptInput.value.trim(); if (!text || sendButton.disabled) { return; } addMessage(text, "user"); promptInput.value = ""; sendButton.disabled = true; const responseBox = addMessage("Thinking...", "ai"); try { const response = await fetch("/api/chat", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ message: text }) }); const data = await response.json(); if (!response.ok) { throw new Error( data.error || "Something went wrong." ); } responseBox.textContent = data.reply; } catch (error) { responseBox.textContent = "Error: " + error.message; } finally { sendButton.disabled = false; promptInput.focus(); } } function newChat() { chat.innerHTML = ` <div id="welcome" class="welcome"> <h1>How can I help you?</h1> <p>Ask Cause AI anything.</p> </div> `; promptInput.focus(); } promptInput.addEventListener("keydown", function(event) { if (event.key === "Enter") { sendMessage(); } }); 